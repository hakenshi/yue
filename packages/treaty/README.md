# treaty

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This package centralizes Eden Treaty usage so both the CLI and web UIs can consume the same typed client.
